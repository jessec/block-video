{namespace site.block}
/**
 * Says hello to the world.
 * @param url
 * @param content
 * @param position
 */
{template .photo}
  <article class="block photo {$position}" data-type="photo">
    <div>
      <img src="{$url}">
    </div>
    <p>{$content|noAutoescape}</p>
  </article>
{/template}